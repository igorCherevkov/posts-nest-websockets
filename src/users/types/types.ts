export type ReturningData = {
  id: string;
  login: string;
  email: string;
  userImg: string | null;
  token: string;
};

export type RequestingData = {
  id: string;
  login: string;
  email: string;
  userImg: string | null;
};
